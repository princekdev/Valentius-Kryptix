# Data

No local dataset file is stored in this repo. The project loads the
**Breast Cancer Wisconsin (Diagnostic)** dataset directly from
`sklearn.datasets.load_breast_cancer()` at runtime, so the pipeline is
fully reproducible without downloading or committing any data files.
